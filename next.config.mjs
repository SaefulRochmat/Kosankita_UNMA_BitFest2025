/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'www.cic.ac.id',
            'images.pexels.com'
        ],
    },
};

export default nextConfig;
